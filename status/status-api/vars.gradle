import static java.lang.System.getenv

ext {
    artifactName = 'status-api'
    userbase = 'comroid-git'

    projectDescription = 'The API for the comroid Status Server'
    exportedProjects = project(':status').subprojects.collect { it.name }
}
